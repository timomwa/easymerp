<%= form_for @vehicle_make do |f| %>
	
	<%= render 'shared/errors', object: @vehicle_make %>
  
  <p>
    <%= f.text_field :name,  class: "form-control", placeholder: "Name", required: true %>
  </p>
  <p><%= f.submit "Create", class: "btn btn-lg btn-primary btn-block btn-signin" %></p>
<% end %>