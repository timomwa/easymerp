<h1>Listing users</h1>
<%= link_to 'New user', new_user_path %>
<table>
  <tr>
    <th>Username</th>
    <th>Email</th>
  </tr>
 
  <% @users.each do |user| %>
    <tr>
      <td><%= user.username %></td>
      <td><%= user.email %></td>
    </tr>
  <% end %>
</table>