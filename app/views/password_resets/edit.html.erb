 <div class="container">
        <div class="form-signin">
            <div class="card card-container">
            	<div class="formList">
            	
            	
				
					<%= form_tag({controller: "password_resets", action: "update"}, method: "patch")  %>
				  		<p>
				    		<%= password_field_tag(name="user[password]" , "", class: "form-control", placeholder: "Password") %>
				    	</p>
				    	
				    	<p>
				    		<%= password_field_tag(name="user[password_confirmation]", "", class: "form-control", placeholder: "Password Again") %>
				    	</p>
				  		<p>
				    		<%= submit_tag "Reset Password", class: "btn btn-lg btn-primary btn-block btn-signin" %>
				  		</p>
				  		
				  		<!--
				  		<%= form_tag("/password_resets", method: "patch") %>
				  		
				  		<p>
				  		    <%= hidden_field_tag(:id,  params[:id] ) %>
				    		
				    	</p>
				  		
				  		
                
				  		<p>
				    		<%= submit_tag "Reset Password", class: "btn btn-lg btn-primary btn-block btn-signin" %>
				  		</p>
				  		
				  	-->
				  	
				  	
				</div>
			</div>
		</div>
</div>